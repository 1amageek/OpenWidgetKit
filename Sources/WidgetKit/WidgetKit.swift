// FIXME(INCOMPLETE_IMPLEMENTATION): This target currently exposes the initial timeline value/provider
// surface and runtime validation only. StaticConfiguration, WidgetCenter, provider completion
// ownership, scheduling, and Windows host integration remain incomplete. Do not report WidgetKit as
// usable until an unmodified widget is exercised on the real Windows Widgets Board.

@_exported import OpenFoundation
@_exported import SwiftUI
import OpenWidgetRuntime
