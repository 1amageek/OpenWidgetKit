// FIXME(INCOMPLETE_IMPLEMENTATION): This target currently exposes only the environment-value substrate
// required by TimelineProviderContext. The production path will add the widget-scoped View and
// WidgetConfiguration surface. Do not report SwiftUI source compatibility until fixtures and
// behavior tests cover every declaration advertised in IMPLEMENTATION_PROGRESS.md.

@_exported import OpenFoundation
import OpenWidgetRuntime
